<div class="container" style="padding: 50px; background-color: #e8c06f; border-radius: 15px;">
	<h2> Your Order Booked Successfully!!! </h2>
	<br>
	<h5>Thanks for shopping! Come again ....</h5>
	<br>
	
	<h5>Remaining Points : <%= current_user.points %></h5>
	<br>
	<h4 class="btn btn-outline-success"><%= button_to "Home", order_path(@current_order), method: :delete, data: { turbo_confirm: "Are you sure?" } %></h4>
</div>